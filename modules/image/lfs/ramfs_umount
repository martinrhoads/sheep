

set -e
ramfs_mount_point=$(pwd)/ramfs
if mount | grep --quiet $ramfs_mount_point; then 
  echo unmounting ramfs from $ramfs_mount_point
  sudo umount $ramfs_mount_point
else
  echo no ramfs is mounted on $ramfs_mount_point
  exit 0
fi