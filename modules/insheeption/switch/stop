export SHEEP_PARENT_MODULE_DIR=$(dirname $SHEEP_PARENT_MODULE_DIR)
require insheeption build

# -*- mode: shell-script;  -*-

# this will be replaced with something like an api call

if pgrep vde_switch >> /dev/null; then
  sudo pkill vde_switch
fi
